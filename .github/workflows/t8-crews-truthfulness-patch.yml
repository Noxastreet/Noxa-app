name: Apply T8 Crews truthfulness patch

on:
  push:
    branches:
      - fix/audit-t8-core-truth-accessibility

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/audit-t8-core-truth-accessibility
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Apply exact scoped replacements
        run: node scripts/apply-t8-crews-truthfulness.mjs
      - name: Verify T8 core contract
        run: node scripts/verify-t8-core-truth-accessibility.mjs
      - name: Verify patch whitespace
        run: git diff --check
      - name: Remove temporary patch helper
        run: git rm scripts/apply-t8-crews-truthfulness.mjs
      - name: Commit patched runtime files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix(t8): remove unsupported Crew proximity claims"
          git push origin HEAD:fix/audit-t8-core-truth-accessibility
